{% extends 'base.html.twig' %}

{% block title %}
  Niveaux
{% endblock %}

{% block body %}
  {{ parent() }}
  <div class="container-fluid">
      <div class="bg-light mt-5 w-100 d-flex">
        <div id="navNiveau" class="d-flex flex-grow-1 flex-row rounded p-3">
          <nav class="w-100 d-flex">
            <a href="{{ path('niveau_show', { nomOnglet: 'new' }) }}" class="py-2 flex-fill text-lg font-medium text-white bg-primary hover:bg-primary-dark text-center" data-turbo-frame="niveau_tray">Créer un niveau</a>
            <a href="{{ path('niveau_show', { nomOnglet: 'list' }) }}" class="py-2 flex-fill text-lg font-medium text-white bg-primary hover:bg-primary-dark text-center" data-turbo-frame="niveau_tray">Liste des niveaux</a>
          </nav>
        </div>
      </div>
      <div class="flex-grow-1 m-2">
        <div class="w-100 p-3 rounded shadow-sm bg-light">
          <turbo-frame id="niveau_tray" src="{{ path('niveau_show', { nomOnglet: app.session.get('niveau_active') ? app.session.get('niveau_active') : 'new' }) }}">
            <p class="text-lg text-center text-muted">Chargement...</p>
          </turbo-frame>
        </div>
      </div>
  </div>
{% endblock %}
