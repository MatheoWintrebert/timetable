{% extends 'base.html.twig' %}

{% block title %}
  Classes - Gestion
{% endblock %}

{% block body %}
  <div class="management-container">
    <!-- En-tête de la page -->
    <div class="page-header">
      <div class="header-content">
        <div class="header-icon">🎓</div>
        <div class="header-text">
          <h1 class="page-title">Gestion des Classes</h1>
          <p class="page-subtitle">Créez et gérez les emplois du temps de vos classes</p>
        </div>
      </div>
    </div>

    <!-- Navigation des onglets avec Stimulus -->
    <div class="navigation-section">
      <div class="nav-container">
        <nav class="tab-navigation" data-controller="tabs" data-tab-group="classe" data-tabs-active-class="active">
          <a href="{{ path('classe_show', { nomOnglet: 'new' }) }}" class="nav-tab" data-tabs-target="tab" data-tab-id="new" data-action="click->tabs#selectTab" data-turbo-frame="classe_tray">
            <span class="tab-icon">➕</span>
            <span class="tab-text">Créer une classe</span>
          </a>
          <a href="{{ path('classe_show', { nomOnglet: 'list' }) }}" class="nav-tab" data-tabs-target="tab" data-tab-id="list" data-action="click->tabs#selectTab" data-turbo-frame="classe_tray">
            <span class="tab-icon">📋</span>
            <span class="tab-text">Liste des classes</span>
          </a>
        </nav>
      </div>
    </div>

    <!-- Contenu principal -->
    <div class="content-section">
      <div class="content-container">
        <turbo-frame id="classe_tray" src="{{ path('classe_show', { nomOnglet: app.session.get('classe_active') ? : 'new' }) }}">
          <div class="loading-state">
            <div class="loading-spinner"></div>
            <p class="loading-text">Chargement en cours...</p>
          </div>
        </turbo-frame>
      </div>
    </div>
  </div>
{% endblock %}
