<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #040A10;">
  <div class="container">
    <a class="navbar-brand fw-bold" href="{{ path('app_main') }}"><img src="{{ asset('assets/logo.png') }}" alt="Logo" class="img-fluid" style="max-width: 50px;" /></a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>

    <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
      <ul class="navbar-nav mb-2 mb-lg-0 gap-2">
        {% if app.user is defined and app.user %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_account_manager') }}">Mon Compte</a>
          </li>

          {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_admin') }}">Admin</a>
            </li>

            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownOptions" role="button" data-bs-toggle="dropdown" aria-expanded="false">Gestion</a>
              <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="navbarDropdownOptions">
                <li>
                  <a class="dropdown-item" href="{{ path('app_niveau') }}">Niveaux</a>
                </li>
                <li>
                  <a class="dropdown-item" href="{{ path('app_classe') }}">Classes</a>
                </li>
                <li>
                  <a class="dropdown-item" href="{{ path('app_matiere') }}">Matières</a>
                </li>
                <li>
                  <a class="dropdown-item" href="{{ path('app_professeur') }}">Professeurs</a>
                </li>
                <li>
                  <a class="dropdown-item" href="{{ path('app_programme') }}">Programmes</a>
                </li>
                <li>
                  <a class="dropdown-item" href="{{ path('app_emploi') }}">Emploi du temps vide</a>
                </li>
              </ul>
            </li>
          {% endif %}
        {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
          </li>
        {% endif %}
      </ul>

      {% if app.user is defined and app.user %}
        <form method="post" action="{{ path('app_logout') }}" class="d-flex">
          <button class="btn btn-outline-light btn-sm" data-turbo="false">Se déconnecter</button>
        </form>
      {% endif %}
    </div>
  </div>
</nav>
